spring.datasource.url=jdbc:mysql://localhost:3306/coffee?useSSL=false&serverTimezone=GMT&characterEncoding=utf-8&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=zhan0616
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.resources.static-locations=classpath:/

mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=team.bxbz.caffee.entity

server.port=8080

server.tomcat.uri-encoding=UTF-8

spring.servlet.multipart.maxFileSize=100MB
spring.servlet.multipart.maxRequestSize=1GB


# Hikari will use the above plus the following to setup connection pooling
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
#连接池保持最小空余连接数量
spring.datasource.hikari.minimum-idle=5
#连接池允许的最大连接数量
spring.datasource.hikari.maximum-pool-size=100
#update自动提交设置
spring.datasource.hikari.auto-commit=true
#连接空闲生命周期设置
spring.datasource.hikari.idle-timeout=30000
#连接池名称
spring.datasource.hikari.pool-name=DatebookHikariCP
#检查空余连接优化连接池设置时间,单位毫秒
spring.datasource.hikari.max-lifetime=1800000
#设置连接超时时间设置
spring.datasource.hikari.connection-timeout=30000
#连接查询语句设置
spring.datasource.hikari.connection-test-query=SELECT 1
